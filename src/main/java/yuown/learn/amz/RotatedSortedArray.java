package yuown.learn.amz;

public class RotatedSortedArray {
	public static void main(String[] args) {
		int[] a = new int[] { 5, 6, 1, 2, 3, 4 };
		int min = findMin(a, 0, a.length);
		System.out.println("Minimum: " + a[min]);
	}

	private static int findMin(int[] a, int start, int length) {
		int min = -1;
		for (int i = start; i < length; i++) {
			if(start - 1 >= 0) {
				
			}
		}
		return min;
	}
}
